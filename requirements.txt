# WeAuto v3.25.1 — 依赖清单（已升级支持微信 4.x）
# 安装：venv\Scripts\python -m pip install -r requirements.txt -i https://mirrors.aliyun.com/pypi/simple/
# 注意：全部走二进制 wheel（--only-binary=:all:），不本地编译。

# ===== Web 配置后台 (config_editor.py, 端口 5001) =====
flask
flask-cors
flask-wtf
flask-limiter
sqlalchemy~=2.0.37
werkzeug
waitress
psutil
filelock
requests
beautifulsoup4
lxml

# ===== LLM / 辅助 =====
openai==1.84.0

# ===== MCP（供 Codex / WorkBuddy 等 agent 以 MCP 协议调用 WeAuto）=====
# 注意：固定 <2，v2 把 FastMCP 改名为 MCPServer 且 API 大改；本项目的 weauto_mcp.py 基于 v1 FastMCP。
mcp>=1.9,<2

# ===== 微信 4.x 引擎原生依赖（wechatauto，vendor/ 内已打 WAL/方向补丁）=====
# 缺一个都跑不起来
cryptography      # 解密微信 4.x 加密 SQLite 聊天记录
zstandard         # WAL 日志解压
pypinyin          # 联系人拼音/搜索
uiautomation      # UI 自动化（发消息/控窗口核心）
comtypes          # uiautomation 依赖（COM）
pyperclip         # 剪贴板读写（发消息走剪贴板）
pywin32           # 发送前把微信窗口置前/聚焦
Pillow            # 图片处理
typing-extensions
colorama          # wechatauto/logger.py 顶层 import（CI 干净 venv 缺失会直接导致 PyInstaller 分析期 ModuleNotFoundError 构建失败）
imageio-ffmpeg    # wechatauto/media.py 用 ffmpeg 处理音视频（可选但建议装，避免运行时缺 ffmpeg）

# ===== 可选（缺失自动降级，不影响核心收发）=====
# winsdk : 图片 OCR 文字识别（仅 wechatauto.guia.ScreenOCR 用到，且为懒加载 import）。
#          注意：winsdk 目前(2026-09)无 CPython 3.13 的 wheel，故从硬性依赖移除；
#          若需图片/表情 OCR 且运行在 Python 3.9~3.12，可自行 pip install winsdk。
# pyautogui : 旧版表情截图识别（bot.py 已改为可选导入，未使用）
# wxauto / wxautox-wechatbot : 旧版微信3.x 引擎，本项目已用 vendor/wechatauto 取代，无需安装。

# ===== 可选（缺失自动降级，不影响核心收发）=====
# pyautogui : 旧版表情截图识别（bot.py 已改为可选导入，未使用）
# winsdk    : 图片 OCR 文字识别（仅 wechatauto.guia.ScreenOCR 用到）
