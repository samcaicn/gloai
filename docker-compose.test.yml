services:
  postgres-test:
    image: postgres:17-alpine
    environment:
      POSTGRES_USER: ceoadmin
      POSTGRES_PASSWORD: ceoadmin
      POSTGRES_DB: ceoadmin_test
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U ceoadmin"]
      interval: 2s
      timeout: 3s
      retries: 10
    ports:
      - "15432:5432"
    tmpfs:
      - /var/lib/postgresql/data
