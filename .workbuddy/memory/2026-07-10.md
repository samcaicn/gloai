# 2026-07-10 工作日志

## 左侧菜单「技能」入口修复
- 问题：左侧 NavPanel 的 `SkillsNavSection` 头部「技能」原本是静态 `<div>`（`cursor: default`、无点击处理），只有下面的单个技能行可点；顶栏 tab 的技能可正常打开场景。
- 修复：
  - `src/web-ui/src/web-ui/src/app/components/NavPanel/sections/skills/SkillsNavSection.tsx`：把头部改成可点击 `<button>`，`onClick → openScene('skills')`（来自 `useSceneManager`，含 `activeTabId` 用于高亮），即使技能列表为空也始终可点。
  - 同级 `.scss`：`is-always-open` 改为 `cursor: pointer`，新增 `.is-active` 高亮，并重置原生 button 的 border/font。
- 验证：`tsc --noEmit` 无新增报错（其余 3 个错误为既有、与本改动无关）。

## 去掉定时任务「未配置模型」提示
- 需求：本客户端不需配置模型，均通过 MCP 请求服务器由服务器自动匹配模型，故 TasksScene 顶部「未配置模型 — 请在设置中选择一个模型」的红色告警徽章及触发按钮的相关禁用提示应移除。
- 修改 `src/web-ui/src/web-ui/src/app/scenes/tasks/TasksScene.tsx`：
  - 删除头部 `modelChecked && <span model-badge>` 徽章块（不再显示 modelReady/noModel）。
  - 删除 `modelChecked` state 及 loadModel 里的 `setModelChecked(true)`（否则 noUnusedLocals 报错）。
  - 触发按钮 className 去掉 `is-disabled-reason`，title 恒为 `t.trigger`。
  - 保留 `modelConfig`（仍作为 `cronLocalTrigger({ id, modelConfig })` 参数传递，可为 null，服务器侧处理）。
- 前端产物：vite build → 根 `dist/`。构建命令 `pnpm --dir src/web-ui/src/web-ui build`（corepack 未装时用 `node node_modules/vite/bin/vite.js build`）。

## 按钮功能全面审计 + 本期开发（tupAI Tauri 客户端）
- **审计方法**：脚本把全部前端 `invoke('cmd')` 与 `lib.rs` 注册的 334 个命令做交叉比对。
  - 结论：**tupai 自有桥接层 `infrastructure/api/tupai/*` 缺失命令 = 0**（全部接通）。
  - **缺失共 323 个，100% 来自 BitFun 克隆层 `infrastructure/api/service-api/*`**（及其 LSP/终端/编辑器/ssh-remote/快照/Git/MiniApp 工具层）——属已剔除范围的编程/助理功能，`api.invoke` 永远走 Tauri 但后端无实现。
  - 桌面宠物 `resize_agent_companion_desktop_pet` / `delete_agent_companion_pet_package` 未注册（用户要求 ⏸ 保留）。
- **已实现的真功能（后端新增/补全）**：
  - 公告系统 `commands/announcements.rs`（6 命令 + 内置卡片 + 版本化持久化 `announcements_state.json`）+ MCP `client.check_update` → 更新提示卡片。
  - 任务完成展示 `commands/notification.rs`（`send_system_notification`，`tauri-plugin-notification` 已 init）。
  - 浏览器 `commands/browser.rs`（`browser_webview_eval` / `browser_get_url`，按 label 在原生 webview 执行 JS / 读 URL）。
  - WhatsApp `get_whatsapp_qrcode` 结构化返回 + 前端 `im.ts`/`ImSettingsTab` 接线。
  - 技能任务队列 `commands/task_queue.rs`（`enqueue`/`list`/`cancel` 内存+持久化 `skill_queue.json`）。
  - `configure_broker`（`pc_automation.rs`）落盘 `brokers.json`，`list_brokers` 合并。
- **改称呼 UIRAP → UIRPA**：全代码库改名（模块 `uirap.rs`→`uirpa.rs`、命令 `uirap_*`→`uirpa_*`、`UirapState`→`UirpaState`、事件 `uirap_*`→`uirpa_*`），保留设计文档 `uirap改造技术方案.md`/`uirap v2` 引用。13 命令注册，其中 5 个已真实实现、8 个仍为 stub（无 UI，⏸ 暂不接）。
- **本轮新增修复的真 bug**：`show_main_window` → `fw_show_main_window`（App.tsx:218 + 桌面宠物 :340/:435，否则点击托盘/宠物唤起主窗口时 `command not found`）。
- **编译关键坑（Tauri 2.11.5）**：`Manager::get_webview`/`app.manager()` 均受 `unstable` gate 或私有字段限制；正确做法是 `use tauri::Manager` + `app.get_webview_window(label)` 拿到 `WebviewWindow`，其自带 `eval()`/`url()`。`Webview` 是带运行时泛型的结构体，必须写 `Webview<tauri::Wry>`。
- 报告：`D:/1data/ai/tupautochromium/按钮功能检查报告.md`（v3）。

## LLM 会话改走 MCP（修复「初始化失败 + LLM 流式 404」）
- **根因**：`commands/llm_stream.rs` 直接 POST `https://ai.tuptup.top/v1/chat/completions`，但云端该 OpenAI 兼容端点已下线，返回 `HTTP 404 {"detail":"Not Found"}`（已用 curl + OpenAPI `/openapi.json` 证实；云端真实聊天 API 是 `POST /api/v1/ilink-chat/send` + `GET /api/v1/sse/chat/{session_id}`，需 `Authorization: Bearer`）。前端 `TupaiChatScene` 初始化调用 `llmStreamChat` 即触发该 404。
- **决策**：按用户要求「去掉 `/v1/chat/completions` 全部相关代码，LLM 必须走 MCP」。
- **改动**：
  - `src/web-ui/.../infrastructure/api/tupai/llm.ts`：重写 `llmStreamChat` → `invoke('mcp_call_v2', {action:'llm.stream_request', params:{session_id,messages,stream:true,model?}, timeoutSecs:120, token})`；解析 `{ok,data:{content}}` 为一个 content chunk（data=字符串）+ done chunk，保持 `AsyncGenerator<LlmStreamChunk>` 契约（TupaiChatScene 按 `chunk.data` 为字符串消费）。
  - 删除 `src-tauri/src/commands/llm_stream.rs`；`commands/mod.rs` 去掉 `pub mod llm_stream;`；`lib.rs` 去掉 `commands::llm_stream::llm_stream_chat,` 注册。
  - `src-tauri/src/skills/capabilities.js`：删除直连云端的 `fetch('https://ai.tuptup.top/v1/chat/completions')` fallback（保留 MCP 主路径 + 注入 provider 链）。
  - `hermes/llm_service.rs`：删除死代码 `openai_complete`（硬编码 `/v1/chat/completions`），并把「云端只支持 chat-completions」等过时注释改为「云端 LLM 统一走 MCP `llm.stream_request`；`openai_stream_bytes` 仅用于 self-hosted/on-prem OpenAI 兼容提供商」。
  - `全面改写计划.md`：两处 `llm_stream_chat` 文档更新为 MCP `llm.stream_request`。
- **未动**：hermes 嵌入式服务器本地 `/v1/chat/completions`、`/v1/responses` 路由与 `legacy.rs` 本地网关（均为 self-hosted/本地，非云端；剔除会破坏自进化 agent 与 legacy 网关）。
- **验证**：`cargo check` 后台运行中（项目从零编译，首次较慢）。静态核查：Rust 无 `LlmStreamChunk`/`llm_stream` 残留引用，`openai_complete` 无调用方。

## 顶栏右侧 EN/中 与亮/暗色切换「真正实现」
- **根因**：`src/web-ui/.../app/components/SceneBar/SceneBar.tsx` 的亮/暗按钮原本驱动一套**已废弃的 scheme 系统**（`@/infrastructure/api/tupai/theme.ts`：写 `localStorage('tupai-scheme')` + `data-scheme` 属性 + 派发 `tupai-scheme-change`）。但真实主题引擎是 `ThemeService`（`infrastructure/theme/core/ThemeService.ts`），它在 `:root` 注入全部 CSS 变量（内联样式，优先级最高）并设 `data-theme`/`data-theme-type`；scheme 系统的 `data-scheme` 只被遗留的 `app/styles/tupai-theme.scss` 用来叠一层赛博朋克发光，对整体配色无控制权 → 点击亮/暗毫无可见变化。语言方面，`LanguageSelector mode="icon-only"` 的触发 `IconButton` **没有 onClick**，顶栏无法切换语言。
- **修复**：
  - 亮/暗：`SceneBar` 改用 `useTheme()`（`@/infrastructure/theme`），点击 `setTheme(isDark ? 'bitfun-light' : 'bitfun-dark')`，真正切换并持久化主题；同时 `removeAttribute('data-scheme')` 清除遗留覆盖。移除 scheme 相关 import/state/`onSchemeChange` 订阅。
  - 语言：删除 `LanguageSelector`，改为直接切换按钮（显示 `中`/`EN`），点击 `changeLanguage(zh-CN <-> en-US)`（来自 `useI18n('common')`），与 `app/layout/NavBar.tsx` 的 EN/中 行为一致。新增 `SceneBar.scss` 的 `.bitfun-scene-bar__lang-btn` 样式。
- **注意**：`main.tsx` 启动时仍会设 `data-scheme`（默认 `cyberpunk-cyan`）作为首屏遗留覆盖，仅影响发光/字体/选区等少量叠加样式；用户一旦点击切换即被清除，主题完全由 `ThemeService` 接管。`tupai-theme.scss` 与 `theme.ts` 这套 scheme 系统目前已成死代码（仅首屏残留），如需彻底清理可后续移除。
- **验证**：`tsc --noEmit` 无 SceneBar 相关报错；全仓仅 2 个既有错误（FloatingWindow.tsx、ImSettingsTab.tsx），均与本次改动无关。

## 流程图只在「会话区新入口页面」显示（不再内嵌自动化页）
- **需求**：流程图不能显示在自动化页（AutomationScene）内，只能显示在「会话区新入口」（点侧栏"最近录制"项打开的独立 `FlowchartScene` 页面）内。
- **原状**：`app/scenes/automation/AutomationScene.tsx` 录制/执行完成后，既 `setFlowchart(...)` 内嵌显示 `automation-scene__flowchart-section`（FlowchartView/EditableFlowchartView），又 `pushRecordingHistory` 生成侧栏入口 → 流程图两处都显示。独立页 `FlowchartScene`（scene id `flowchart`）通过 `MainNav` 的"最近录制"项 `handleOpenRecording`（写 `sessionStorage tupai:flowchart:selectedApp` + 派发 `tupai:flowchart:selectApp` 事件 + `openScene('flowchart')`）打开。
- **修复**（仅改 `AutomationScene.tsx`）：
  - 删除内嵌流程图区 JSX 及相关 state（flowchart/editMode/editedFlowchart/selectedNodeId/flowchartBusy/flowchartError）、handler（handleStopRecord/handleLoadRecording/handleExecuteFlowchart）、import（FlowchartView/EditableFlowchartView、mergeFlowcharts、recordingStop/automationExecute、Circle/Play 图标、createLogger/log、currentLanguage/locale）。
  - 新增 `openFlowchartScene(appName)`（复用 `useSceneManager().openScene`，与 MainNav 打开流程图逻辑一致）。startExecute/handleRecord 轮询完成分支改为 `pushRecordingHistory + openFlowchartScene`，不再 setFlowchart。录制仍由 RecorderToolbar 悬浮窗自动 start/stop。
- **验证**：`tsc --noEmit` 无 AutomationScene 报错（其余 FloatingWindow/ImSettingsTab/useInstalledSkills 报错为既有、与本改动无关）。

## 技能页「导入」按钮 + 模态框（仅导入，其他 UIRPA 功能不动）
- **需求**：只在技能页右上角加「导入」按钮，模态框选 SKILL.md 导入，导入后固定展示在技能列表首位、其后顺位。明确不做 UIRPA 的 export/encrypt/decrypt/delete/execute/list（import 已做，余 7 个 stub 仍不动）。
- **后端**：`src-tauri/src/commands/uirpa.rs` 重写 `uirpa_import_skill(app, skill_md)`：
  - 解析 YAML frontmatter（name/description/version），缺省回退首个 `# 标题`，再回退 `imported-skill`。
  - `derive_skill_id` = `<slug>-<8hex时间戳>`；持久化到 `app.path().app_data_dir()/skills/<id>/SKILL.md`（需 `use tauri::Manager` 才能调 `app.path()`）。
  - 返回 `SkillMeta { skill_id, version, intent:description, updated_at:RFC3339(chrono::Utc), success_rate:0 }`。
  - 前端 `invoke('uirpa_import_skill', { skillMd })` → camelCase 映射 `skill_md`。
- **前端**：
  - 新增 `app/scenes/skills/hooks/importedSkills.ts`：localStorage（`tupai.importedSkills.v1`）按最新优先存 `ImportedSkill[]`（= SkillInfo + addedAt），`addImportedSkill` 去重并置顶，`getImportedSkills` 读取。
  - `useInstalledSkills.ts`：`loadSkills` 拉取后 `setSkills([...imported, ...list])` 把导入技能置顶；导出 `parseSkillNameDesc`；新增 `handleImport(filePath, rawMarkdown)`（调后端 → 写导入列表 → 刷新）。用 `@tauri-apps/api/core` 的 `invoke` + `@tauri-apps/plugin-fs` 的 `readFile`。
  - `SkillsScene.tsx`：工具栏右上角加「导入」按钮（`Upload` 图标）→ 模态框（`@/component-library` 的 `Modal`）：选 .md/.mdc 文件 → `readFile`+`TextDecoder` 读内容 → 前端解析预览 name/description → 确认调 `handleImport`。导入技能 `level:'user'`、`isBuiltin:false`、`path:''`（避免无效 reveal）。
  - i18n：`locales/{en-US,zh-CN,zh-TW}/scenes/skills.json` 加 `toolbar.importTooltip`、`messages.importSuccess/importFailed`、`importModal.*`。
  - SCSS：新增 `.skills-main__import-btn`（镜像 add-btn 描边样式）与 `bitfun-skills-scene__import-form/preview/...`。
- **坑**：`AppHandle::path()` 是 `Manager` trait 方法，需 `use tauri::Manager`；`chrono::Utc::now().to_rfc3339()` 需 chrono 的 `clock` feature（Cargo.toml 已开）。
- **验证**：`cargo check` + `tsc --noEmit`（待跑）。

## 定时任务（cron_local）健壮性加固
- 需求：「定时任务功能 保证 健壮」（前次会话已落地，本次续会复核）。
- 改动文件：后端 `src-tauri/src/hermes/cron_local.rs`、`src-tauri/src/commands/mcp_proxy.rs`、`src-tauri/src/lib.rs`；前端 `src/web-ui/.../infrastructure/api/tupai/cron.ts`、`src/web-ui/.../app/scenes/tasks/TasksScene.tsx`。
- 修复的真实 bug：
  1. **LLMService 强制 api_key 与「服务器自动匹配模型」架构冲突**：cron_local 原本走 `LLMService`（需 api_key），与本客户端「全部经 MCP、服务器自动选模型」声明直接矛盾 → 所有定时任务必失败并报「未配置」。改为复用 `mcp_proxy::mcp_call_v2_inner` 的 `llm.stream_request` 路径（Bearer = device_token）。
  2. **cron 时区错位**：前端按本地墙钟填 cron 表达式（如 `0 8 * * *`=每天本地 8 点），但后端 `compute_next_fire` 用 `Utc::now()` 计算 → 实际在本地 16:00（中国）触发。改为 `Local::now()` 计算墙钟、UTC 瞬间落盘（`.with_timezone(&Utc).to_rfc3339()`）。
- 健壮性加固：
  - 持久化错误可见化：`create`/`delete`/`trigger` 的 `persist_jobs` 与 `append_run` 错误由静默 `_` 改为 `log::error!`。
  - 补跑防惊群：`run_due_jobs` 触发前 `tokio::time::sleep(300ms)` 错位。
  - token 生命周期：前端 mount / window focus / visibilitychange 刷新 + 后端 `CronLocalState.token` 内存缓存（非持久化）；新增 `cron_local_set_token` 命令；`effective_token = 显式优先 or 缓存`。
- 编译校验（前次会话）：`cargo check --lib` 与 `tsc --noEmit` 本次改动两侧均零新增报错；唯二失败为既有、跨文件无关（Rust: `src/recording/store.rs` 的 `'\u{1f}'` char 分隔符因 rustc 1.97 新版移除 `impl Borrow<str> for char`；TS: `FloatingWindow.tsx`/`AutomationScene.tsx`/`ImSettingsTab.tsx` 的 3 个错误）。本次续会通过 grep/read 复核上述改动均落地无误。

## 自动化页：软件操作从「悬浮窗」改为「右侧页内面板」
- **需求**：点击自动化页内的软件，不再用悬浮窗（fwOpen 的独立 Tauri webview）展示操作，改为在自动化页右侧页内面板展示；空间够，要设计好，并能统计录制步骤、执行信息等。
- **改动**（仅 `app/scenes/automation/AutomationScene.tsx` + `.scss` + 两处 `locales/*/common.json` 的 `automationScene` 段）：
  - 布局从单列改为 **master-detail**：`automation-scene` 变 `flex-direction: row`，新增 `automation-scene__main`（左：标题/搜索/计数/卡片网格）与 `automation-scene__detail`（右：固定 360px 操作面板，窄屏 <760px 自动改为上下堆叠）。
  - 删除原居中模态弹窗（`popup` state、`PopupState`、`handleSoftwareClick` 里 `getBoundingClientRect` + `fwOpen` 悬浮逻辑），改为 `selected` state：点卡片即选中并在右侧面板展示；选中卡片高亮 `.is-active`。
  - 右侧面板内容：① 头部（图标/名称/关闭×）；② 统计网格（录制步骤 `totalActions`、录制批次 `totalBatches`、使用次数、最近录制 `lastRecordDate`，数据来自 `getAppStats`）；③ 目标输入；④ 操作按钮（执行 / 查看流程图）；⑤ 执行中状态条（运行指示 + 停止）；⑥ **录制控制面板**（内嵌 `RecorderPanel` 子组件，直接调 `recordingStart/Stop/Pause/Resume` + 轮询 `recordingGetStatus`，显示实时步数与时长，替代原录制悬浮窗 `RecorderToolbar`）；⑦ 日志区。
  - **彻底去掉 `fwOpen`/`fwGetState`**：执行（startExecute）只 `launchSoftware` + 在右面板显示运行状态/日志，并有「停止执行」；录制完全在右面板控制，卸载时若仍在录自动 `recordingStop` 防泄漏；录制完成后 `pushRecordingHistory` + `openFlowchartScene` 跳流程图页。
  - 新增 i18n 键：`detailAria/detailEmpty/closeDetail/statSteps/statBatches/statUsage/statLast/noRecordYet/recorderTitle/recorderElapsed/viewFlowchart/running/stopExecute`（zh-CN + en-US）。
- **验证**：`tsc --noEmit` 无 AutomationScene 报错（全仓仅 2 个既有错误 FloatingWindow.tsx 缺 `RecordingStatusSnapshot` 导出、ImSettingsTab.tsx，均与本次无关）；两份 common.json 解析通过。

## 技能页：下载的技能 + 导入的技能 都支持「本地删除」
- **需求**：技能页里两类非内置技能（从市场下载的、从文件导入的）都要能在本地删除，不只是摆个删除按钮。
- **后端** `commands/uirpa.rs` 的 `uirpa_delete_skill`（原 stub 返回错误）改为**真实落盘删除**：删 `app.path().app_data_dir()/skills/<skill_id>/` 整个目录（与 `uirpa_import_skill` 落盘位置对应），幂等、且 `dir.starts_with(skills_root)` 路径安全校验。命令签名 `uirpa_delete_skill(app, skill_id)`，Tauri 自动剥离 `AppHandle`，集成测试签名断言 `fn(String)->Result<(),String>` 仍成立。
- **前端** `importedSkills.ts`：新增 `removeImportedSkill(key)` 与 `removeImportedSkillById(skillId)`，从 localStorage 移除导入快照。
- **前端** `useInstalledSkills.handleDelete` 分流：
  - `sourceSlot === 'imported'` → `invoke('uirpa_delete_skill', { skillId: skill.dirName })` + `removeImportedSkill(skill.key)`（纯本地、可持久化）。
  - 其它（下载/用户安装）→ `invoke('uninstall_skill', { name: skill.name })`（真实后端 `commands/legacy.rs` 的 `hermes skills uninstall <name>`，本机卸载）。
  - 不再调用缺失的 BitFun 克隆命令 `delete_skill`（该后端命令在本仓未注册），改用真实的 `uninstall_skill`。
- **注意**：技能页 installed 列表当前由 `configAPI.getSkillConfigs` 拉取（后端 `get_skill_configs` 命令本仓未注册，属已剔除的 BitFun 克隆层），故实际可见的可删技能主要是导入类；下载类走真实 `uninstall_skill` 命令。删除按钮显示条件仍是 `!skill.isBuiltin`。
- **验证**：
  - 前端 `tsc --noEmit`：本改动（`uirpa.rs`/前端两 hook 文件）零新增报错；全仓仅 2 个既有错误（`FloatingWindow.tsx` 的 `actionCount`/`action_count`）。
  - 后端 `cargo check`：**`uirpa.rs` 不在报错列表 → 本次改动编译通过**。但 `src/automation/engine.rs` 当时因 `enigo = "0.3"`（lock 0.3.0）API 漂移报错（`MouseControllable`/`KeyboardControllable` 已移除、`Enigo::new()` 改需参数且返回 `Result`、`Key::Layout` 变体删除），该文件在本会话早期自动化开发中被写成旧 API，属**既有构建阻断**。

## 修复 enigo 0.3 API 漂移（`src/automation/engine.rs`）
- **用户提问**：自动化引擎是自研还是取的开源模块？→ 答：引擎状态机/三级重试/事件/数据结构是 **tupAI 全自研**；仅**底层键鼠注入借用开源 crate `enigo`**（0.3.0）。报错纯属 enigo 0.3 破坏性改 API，与引擎无关。
- **enigo 0.3 真实 API（已读 0.3.0 源码确认）**：
  - `Enigo::new(&Settings::default()) -> Result<Enigo, NewConError>`（需 `?`/`unwrap`）。
  - 鼠标：`mouse_move_to`→`move_mouse(x,y,Coordinate::Abs)`；`mouse_click(Button)`→`button(Button, Direction::Click)`。
  - 键盘：`key_sequence`→`text(&str)`；`key_down/key_up(Key)`→`key(Key, Direction::{Press,Release})`。
  - 新增枚举 `Direction::{Press,Release,Click}`（默认 Click）、`Coordinate::{Abs,Rel}`；`Key::{Meta,Return,Shift,Space,Unicode(char),PageUp,PageDown,Insert,CapsLock,...}` 均在。
- **改动**：`perform_step_input` 与其 enigo 调用已适配 0.3（`Enigo::new` + `move_mouse` + `button(Button,Direction)` + `text` + `key(Key,Direction)`），import 补 `Coordinate, Direction`；其它文件 grep 无旧 API 残留（teaching.rs 仅注释）。
- **验证**：`cargo check --message-format=short` 后台运行中（项目首次整机 check 较慢，等待结果）。

## 前端技术栈确认 + 无限画布选型决策
- **前端栈**（来自 `src/web-ui/src/web-ui/package.json` + `vite.config.ts`）：
  - 语言 **TypeScript 5.8**，UI 框架 **React 18.3**，构建 **Vite 7**；
  - 样式 **SCSS(sass) + CSS 变量主题**（ThemeService 注入 `:root`），状态 **Zustand 5 + Immer**，i18n **i18next/react-i18next**；
  - 桌面壳 **Tauri 2**（Rust 后端，`@tauri-apps/api` v2），同一份代码支持 Desktop + Web 两种部署（`build:desktop`/`build:web`）。
- **关键事实**：**React Flow 已直接是依赖** `@xyflow/react ^12.11.2` + `dagre ^0.8.5`，且已在 `app/scenes/automation/flowchart/` 实战使用（`FlowchartViews.tsx`/`FlowchartNodes.tsx`/`flowchartAdapter.ts` 的 `toReactFlow/fromReactFlow` 即为序列化层）。
- **无限画布选型结论**：直接采用文章（一）首选的 **ReactFlow（xyflow）** —— 与本项目 React+TS 栈天然契合、且已落地，无需引入文章（一）次选的 LiteGraph.js（纯 JS，会和 React 渲染模型打架）。文章（二）的成品项目仅作架构参考：① **Toonflow-app**（短剧/分镜无限画布 8k★）重点抄其「画布工程/镜头节点预览/剧情分支的存储逻辑」；② **ComfyUI**（LiteGraph）重点参考其节点缓存/异步任务调度思路（但仅做 AI 生成，无完整剪辑流水线）；③ Open Canvas 参考其本地优先/离线 Docker 部署。
- **落地路径建议**：在现有 `flowchart` 之上扩成独立「无限画布场景」——复用 `flowchartAdapter` 做画布工程持久化，自定义视频/分镜/素材/AI生成/剪辑 节点卡片（TapNow Tapflow 模型），参考 Toonflow-app 的画布工程存储结构。

## Task B 收尾：录制/执行悬浮窗 + 主窗口显隐 + 真实回放（UIA→CDP→OCR→LLM）
- **需求（用户原话）**：
  1. 点击按钮 → 打开悬浮窗 + 隐藏主窗口 + 拉起目标软件。
  2. 执行悬浮窗多个单步按钮。
  3. 自动按 UIA→CDP→OCR→LLM 顺序录制和执行。
  4. 关闭悬浮窗 → 再次打开主窗口的会话区入口 + 加载节点（多次录制去重）+ 可编辑。
- **AskUserQuestion 决策**：持久化到 `recording::store`、统一识别顺序 UIA→CDP→OCR→LLM、连真实回放/点击引擎一起修。
- **根因（旧 bug）**：两套断开的录制落库路径——`teaching.rs::automation::Recorder` 写 `proposal_store`，`recording_cmds.rs` 读 `recording::store`，浮窗关闭后「加载节点」永远空。已统一到 `recording::store`（`save_app_flowchart` + `merge_flowcharts` 按 `node_fingerprint`/`conn_fingerprint` 去重）。
- **后端**：
  - `src-tauri/src/recording/store.rs`：`.join('\u{1f}')` 改 `"\u{1f}"`（E0308 char→&str），节点/连线指纹函数。
  - `src-tauri/src/hermes/cron_local.rs`：克隆 `run_id` 后再 `tokio::spawn`（E0382 use of moved value）。
  - `src-tauri/src/automation/engine.rs`：
    - import 适配 enigo 0.3.0 真实 API：`Enigo::new(&Settings::default())?` / `move_mouse(x,y,Coordinate::Abs)` / `button(Button,Direction::{Press,Release})` / `text(&str)` / `key(Key,Direction)`。
    - `RetryStrategy` 加 `Llm` 变体；`MAX_ATTEMPTS 3→4`；`for_attempt(2)=>Ocr, _=>Llm`。
    - 真实共享 `perform_step_input(step)`：Click→`move_mouse + button(Press/Release)`；Type→`text`；Hotkey→modifiers `key(Direction::Press)` + 30ms + 倒序 `Release`；Wait→`thread::sleep`。
  - `src-tauri/src/automation/engine_test.rs`：`for_attempt(3/99/u32::MAX)` 期望 `Llm`。
  - `src-tauri/src/commands/ext_streams.rs`：`automation_execute` 真实重放（`perform_step_input` 逐节点、300ms 步间隔、计成功/错误），新增 `flowchart_node_to_step`（action click/type/hotkey→InputAction from meta x/y/text/keys），新增 `execute_flowchart_step(node)` 单步命令。
  - `src-tauri/src/commands/floating_window.rs`：新增 `fw_finish_session(app, app_name)` — show main + emit `session:finish-recording` 给主窗口，主窗口收到后 `openScene('flowchart')` + 加载/去重/编辑。补 `serde_json` import。
  - `src-tauri/src/lib.rs`：注册 `fw_finish_session` 与 `execute_flowchart_step`。
- **前端**：
  - `infrastructure/api/tupai/automation.ts`：`executeFlowchartStep(node)` 包装。
  - `infrastructure/api/tupai/floater.ts`：`fwFinishSession(appName)` 包装。
  - `infrastructure/api/tupai/index.ts` 自动 barrel（recording/automation/floater）→ 全部可用。
  - `app/scenes/automation/AutomationScene.tsx`：**整体重写**——删除内嵌 `RecorderPanel` 子组件与轮询；「录制」按钮 = `launchSoftware + fwHideMainWindow + fwOpen({id:'recorder-<app>'})` + `pushRecordingHistory`；「执行」按钮 = `launchSoftware + fwOpen({id:'steps-<app>'})`；日志/统计保留。
  - `app/components/FloatingWindow/FloatingWindow.tsx`：`FloatingWindowKind` 加 `steps`；`resolveKind` 路由 `recorder-`/`steps-` 前缀；`RecorderToolbar` 与新增的 `StepExecutionWindow` 共享 `fwFinishSession` 关闭路径（show main + emit load-nodes 事件）；StepExecutionWindow 调 `recordingLoad` 拉取节点、点单按钮 = `executeFlowchartStep`、依次执行按钮 = 循环 `executeFlowchartStep`。
  - `locales/{zh-CN,en-US}/common.json`：`floatingWindow.recorderTitle` 改带 `{{name}}`；新增 `floatingWindow.startRecording`。
  - `app/scenes/automation/FlowchartScene.tsx`：保存按钮 i18n 键 `flowchartScene.save`（两 locale 都补）。
- **验证**：
  - `cargo check` ✅ Finished `dev` profile in 14m 51s，零错误（首次重编 Tauri + enigo 0.3 + rdev 树较慢）。
  - 前端 `npx tsc --noEmit` ✅ 零新增报错（5 分钟完成 monorepo 全量检查）。
  - Task #1–#12 全部 completed。
- **未做（仅需 UI 联调）**：真实点击 + 节点可视化执行回归（需 Windows 端跑 `pnpm tauri dev` 拉起目标软件做 e2e）。

## 调整识别顺序为 CDP→UIA→OCR→LLM
- **用户要求**：「改为 CDP→UIA CDP 放最前面」（原 UIA→CDP→OCR→LLM）。
- **后端改动**（`src-tauri/src/automation/engine.rs` + `engine_test.rs`）：
  - `RetryStrategy` 枚举顺序：Uia→Cdp 改为 **Cdp→Uia**（CDP 第 0 位）。
  - `for_attempt`: `0=>Cdp, 1=>Uia, 2=>Ocr, _=>Llm`。
  - 测试 `retry_strategy_attempt_zero_is_uia` → `_is_cdp` / attempt_1 `_is_cdp` → `_is_uia` / 注释 `UIA → CDP → OCR → LLM ladder` → `CDP → UIA → OCR → LLM ladder`。
  - 同步修 4 处过时注释：`pc_automation/mod.rs` 文档头、`pc_automation/router.rs` 注释（router 本身是 **domain-aware**：Web 域走 CDP primary / Desktop 域走 UIA primary / OCR fallback / VLM escalation，未改实际控制流）、`pc_automation/apps/types.rs::RoutePreference` 注释、`commands/pc_automation.rs` 头部注释。
- **前端改动**：
  - `flowchartAdapter.ts`：`TECH_TERM_ZH` 加 `llm:'大模型'`；`localizeAction` 正则加 `llm` 关键词。
  - `computerUse.ts`：注释 `strategy = uia/cdp/ocr/vlm` → `cdp/uia/ocr/llm`。
- **验证**：`cargo check` ✅ Finished in 2m 12s，零错误；`npx tsc --noEmit` ✅ 18m 全量检查零错误。
