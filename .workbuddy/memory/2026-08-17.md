# 2026-08-17

- 用户明确项目操作护栏，记入 MEMORY.md（「操作护栏 / 项目规则」节）：① 验证即落地 commit、别停工作区；③ 范围护栏 / 零分支切换，别碰其他分支 / 做 PR；④ GitHub 只起 CI 作用（push 为触发绿 CI，跑完删分支+release，本地=工作台 / GitHub=验证机）。
- 本次只在当前分支 `main` 把规则落成一个 commit，未碰其他分支、未推远程（memory 无 CI 价值，按 ④ 不浪费 push）。
- 注：`main` 上的 raphael-web2api 集成与「keep opc branch」实验已被 revert（436a5df/9c3332d/ce5dda4），属规则 ③/④ 的纠正；其余未提交 WIP（browser_skill、_raph、layer4 改动）不在本任务范围，未动。

## dsh-desktop 「一切皆 plugin」忠实移植（两轮）
- Turn A（commit `a5249b4`，CI `31983851590` 绿）：移植 `.dshpreset` 包生命周期——修 `_convert_dsh_preset`（读 manifest v1 真实字段，非 Preset-Square 服务端 schema）、新增 `plugin_core/preset.py`（导入预览校验 + 导出回环）、安装前校验、preview_install 两步法、registry 同 id 异 source 冲突拒绝、服务/WS `plugin_preview`/`plugin_export`、前端导出按钮 + 安全预览确认弹窗。
- Turn B（commit `1e51c18`，CI `31985834102` 进行中）：移植 **Cordis 分层覆盖 cascade**（dsh「一切皆 plugin」最深的机制）。新增 `plugin_core/cascade.py`：`deep_merge`（dict 递归 / 插件数组按 id 归并 / scalar 覆盖）+ `bundle→preset→profile→home→cli` 五层；preset 层自动从已安装 agent 插件的 `preset/agent.cordis.yml` 组合（装预设即组合运行时）；registry `resolve_effective()`（级联轨迹 trace + missing 标记）与 `sync_effective()`（把解析值写回扁平 profile，运行时即生效）；服务/WS `plugin_cascade`/`plugin_cascade_patch`(仅 home 可写)/`plugin_cascade_reset`；前端「分层覆盖」面板（层级栈 + 生效结果含级联轨迹 + home 覆盖编辑器）。`tests/test_plugin_cascade.py` 8 项过。
- **gitignore 坑**：`.gitignore` 第 30 行 `data/` 会忽略 `opc/plugin_core/data/`（包源码目录），bundle 基线 `bundle.cordis.yml` 用 `git add -f` 强制入库。未来该目录加源文件都需 `-f`。
- **环境事故**：本机 C 盘 100% 满，导致一次 vite 写入把 PluginsPage.tsx 截成 0 字节（已从 `git checkout --` 恢复 HEAD 版后重做 cascade 编辑）；并清出 Temp 垃圾 + `.workbuddy/logs`、`.workbuddy/traces` 释放空间。另 `frontend_src/node_modules/vite` 被清空，已用 node22 的 npm 重装 `vite@7.2.4 --no-save` 修复后构建通过。
- 取舍：Cordis 的 `cordis.patch.yml` **apply 条件**（按已装插件名做条件合并）只做了基础实现（patch-list 形式支持，apply 在 resolver 里 currently 未做条件过滤，直接合并 value）；如需严格 apply 条件语义可后续补。

## 清理本地垃圾 + 强制「本地不能构建」（用户指令：清理本地垃圾 下次本地不能构建）
- 清理：删 `/c/Users/User/.cache`（huggingface 25G + modelscope 5.3G 模型缓存，可再生）、pip 缓存、`/c/Users/User/.npm`；Temp 里 `apply_cascade.py` 与 29 个 `dsh-desktop-profile-*`；仓库内 `tests/_tmp_*`、`.test_*`、`packaging/__pycache__`。磁盘 94%(15G)→84%(39G)，释放约 24G。
- 强制本地不能构建（把规则④从「PyInstaller/NSIS exe」扩展到「一切构建」）：
  - 删本地 exe 构建脚本 `packaging/build.ps1`（CI 不调用，纯本地入口）。
  - 删 `frontend_src/node_modules`（454M，本地前端构建依赖）；`.gitignore` 已忽略 `node_modules/`。
  - 前端 vite 构建搬进 CI：`.github/workflows/opc-build.yml` 在 PyInstaller 前加 `setup-node@v4 (node 22)` + `npm ci` + `npm run build`，产出 `opc/plugins/office_ui/frontend_dist`。
  - 仓库不再提交 `frontend_dist`：`git rm` 移除 + `.gitignore` 忽略 `opc/plugins/office_ui/frontend_dist/`。
- 结论：**本地彻底不能构建**（无 PyInstaller/NSIS 脚本、无 node_modules、无 frontend_dist 源）；任何出包只走 `git push safeopc main:opc` → CI。若本地偶需 `npm run dev` 预览，需先 `npm install`（但禁止 `npm run build` 后提交 frontend_dist）。
- commit：仅含 .gitignore、opc-build.yml、删除 build.ps1 与 frontend_dist、本记忆；不碰无关 WIP（browser_skill/_raph/cli_plugin/opc 其余改动等）。随后 push 验证 CI（CI 需成功跑通新增的 vite 步骤）。
