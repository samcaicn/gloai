# 2026-08-17

- 用户明确项目操作护栏，记入 MEMORY.md（「操作护栏 / 项目规则」节）：① 验证即落地 commit、别停工作区；③ 范围护栏 / 零分支切换，别碰其他分支 / 做 PR；④ GitHub 只起 CI 作用（push 为触发绿 CI，跑完删分支+release，本地=工作台 / GitHub=验证机）。
- 本次只在当前分支 `main` 把规则落成一个 commit，未碰其他分支、未推远程（memory 无 CI 价值，按 ④ 不浪费 push）。
- 注：`main` 上的 raphael-web2api 集成与「keep opc branch」实验已被 revert（436a5df/9c3332d/ce5dda4），属规则 ③/④ 的纠正；其余未提交 WIP（browser_skill、_raph、layer4 改动）不在本任务范围，未动。
