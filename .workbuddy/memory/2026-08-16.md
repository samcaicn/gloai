# 2026-08-16

## CreatorHub 技能「打开页面」流畅度优化
- 实测 `opc-creatorhub open`：瓶颈在 `ensure_venv()` 每次启动都重跑 `pip install -r requirements.txt`（及 `pip install --upgrade pip`），即便 venv 已装好 —— 每次 open 约 12s，其中 ~8s 是无意义的 pip no-op 刷屏。
- 修复：`ensure_venv(force=...)` 改为幂等。新增 provisioning marker（`integrations/creatorhub/.venv/.creatorhub_provisioned`，存 requirements.txt 的 sha256）。marker 命中且 requirements 未变则完全跳过 pip；`--force` 或 requirements 变更才重装。仅 fresh venv / `--force` 时升级 pip。
- `open` 流程加反馈：launching… / already provisioned — skipping install / Waiting for CreatorHub at <url> …（探活打点）/ 就绪。
- `setup` 与 `open` 均加 `--force`（launcher 自身的 setup 也加）。
- 结果：open 12s → ~4s（实测 3.99s）；sidecar 正常启动，`/health` 即时 200，`/` 返回 app/web/index.html（真实 UI）。新增回归测试 test_launcher_provision_marker_idempotent，23 个 creatorhub 测试全过。
- 注意：改动仅本地，尚未 push。`.venv` 已存在但旧代码没写 marker，首次 open 仍会跑一次 pip 并写 marker，之后即快。

## CreatorHub 在内置浏览器打开
- 用户要求：`保证在内置浏览器打开`（即 SafeOPC 桌面 App 的 WebView2 内置浏览器，而非系统默认浏览器）。
- 设计：后端 `office_ui/server.py` 新增 `POST /api/ui/open-browser`（body `{url,title}`）→ 经 `ws_handler.broadcast` 广播 `ui_open_browser` 事件；前端 `wsClient.ts` 加 `onUiOpenBrowser` handler + `App.tsx` 渲染 `InAppBrowser` 覆盖层（iframe + 地址栏 + 外部打开/关闭）。
- CLI `opc-creatorhub open`：先探测 office_ui 端口（`<opc_home>/office_ui.port` → 环境变量 `SAFEOPC_PORT` → 默认 8765），POST 该端点；成功则「在内置浏览器打开」，失败回退 `webbrowser.open`。office_ui 启动时把端口写入 `office_ui.port`（discovery）。
- `opc-build.yml` 直接 PyInstaller 打包**已提交**的 `frontend_dist`（不跑 npm），故必须在本地重建 frontend_dist 并提交，内置浏览器功能才会进 exe。frontend-contracts 仅在 PR/workflow_dispatch 跑，push 不触发。
- 已完成：重建 frontend_dist 并提交（`787e849`，10 files, +345/-65）；push safeopc main（落地）+ opc 分支（触发自清理构建 CI，run 31917237326 in_progress）。25 个 creatorhub 测试全过。

## 前端品牌替换 + 去 GitHub（用户："页面看到的前端部分，都要进行品牌替换。去掉 GitHub 字样和链接。再次CI"）
- 全面排查：前端源码（frontend_src，排除 node_modules）中**唯一可见的 OpenOPC 残留是顶栏 logo**（`App.tsx:2404` 的 `Open<span class="logo-accent">OPC</span>`）；已改为 `Safe<span...>OPC</span>`。其余品牌（window 标题、splash、i18n theme、index.html title、桌面 splash）早已是 SafeOPC。
- GitHub 排查结论：可见前端**没有任何 GitHub 字样/链接**。bundle 里仅有的 2 处 `github` 是 `react-markdown`/`remarkjs` 库内部 doc URL（永不渲染）；源码仅 `SvgIcons.tsx:12` 一处注释引用 github URL（已改为 lucide.dev，去掉 GitHub 链接）。
- `package-lock.json` 里的 github 是第三方依赖的 funding/sponsors 链接（npm 元数据，**不渲染、非 SafeOPC 品牌**，改了会破坏 lockfile 完整性）→ 不动。
- `dist/SafeOPC/.../METADATA` 里的 OpenOPC 是 PyInstaller 构建产物（由 pyproject.toml/README 生成，gitignored，非页面 UI）→ 不动（属文档/打包范围，非「页面」）。
- 重建 frontend_dist（bundle `index-BqU-WQvW.js` → `index-C0a5FwoN.js`）；校验：bundle 内 `OpenOPC`=0、`SafeOPC`=6、`github`=2（react-markdown 内部）。
- 提交 `68590a2`（5 files, +5/-5，dist 视为 rename）；push safeopc main（落地）+ opc 分支（触发自清理构建 CI，run 31917897671 in_progress）。
- 教训：本仓库「前端品牌替换」范围应聚焦**可见 UI 文本**（顶栏 logo、页脚、关于框、外部链接），不要去动 node_modules/package-lock 的依赖元数据与构建产物里的文档字符串。

## 文档全面品牌/链接修正（用户："全面修正文档"）
- 全面排查 SafeOPC 项目文档层：README.md、README.zh-CN.md、docs/*.md（6个）、opc/skills_assets/*/SKILL.md（2个）、.opc/prompts/talent/（100+ agent 模板）。
- 结论：源码与文档层**已基本无 OpenOPC 字样**（前端 rebrand + 早期 rebrand 已覆盖）。唯一属于「SafeOPC 自身 GitHub 字样/链接」且需去掉的是 README.md / README.zh-CN.md 第18-19 行的 Feishu/WeChat 群组徽章（href 指向 github.com/samcaicn/.github/...，自身 GitHub 自链）。已删除这两行。
- 明确不动的部分及原因：
  - `dist/`（PyInstaller 产物，gitignored，CI 重建）—— 非版本控制
  - 第三方开源致谢链接（codex / vibe-kanban / agency-agents / nanobot / pixel-agents，README 793-797）—— 事实性引用，URL 不含 "GitHub" 字样，保留
  - `packaging/` 下 `github.com/samcaicn/safeopc`（SIGNPATH_APPLY.md / fill_signpath*.py / SIGNING.md / ci-template/openopc.policy）—— SignPath 签名所需的真实仓库地址，功能配置，保留
  - `.opc/prompts/talent/` 14 个模板里的 GitHub 字样 —— 第三方导入的 agent 提示词（来自 agency-agents），非 SafeOPC 品牌文档，保留
- 提交 `1ae6e54`（2 files, -4）；push safeopc main（落地）+ opc 分支（触发自清理构建 CI）。

